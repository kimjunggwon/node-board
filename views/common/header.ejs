<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
</head>
<body>
    <nav>
        <% if(!isAuthenticated){ %>
        <a href="/auth">로그인</a>
        <% }else{ %>
        <a href="/logout">로그아웃</a>
        <% } %>
        <a href="/join">회원가입</a>
    </nav>